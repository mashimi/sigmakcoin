[package]
name = "consensus"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
rand = "0.8"
blockchain = { path = "../blockchain" }
proof_verifier = { path = "../proof_verifier" }
